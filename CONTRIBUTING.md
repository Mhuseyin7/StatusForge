# Contributing

Use focused pull requests, add tests for behavioral changes, and run `make lint` and `make test` before submitting. Never commit secrets or production data. Changes affecting authentication, tenant isolation, networking, or webhooks require security-focused review.
